# Backend
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic-settings==2.7.1
python-dotenv==1.0.1
python-multipart==0.0.20

# Telephony (Step 9)
twilio==9.10.9

# Speech-to-text (Step 3)
faster-whisper==1.2.1
# GPU acceleration is optional and NOT listed here - app/stt/whisper_stt.py
# falls back to CPU automatically if no compatible GPU is present (e.g. on
# Railway, which has none). If deploying somewhere with an NVIDIA GPU, add
# nvidia-cublas-cu12 and nvidia-cudnn-cu12 for a real speed improvement -
# they're several hundred MB and pure dead weight anywhere without a GPU.

# Text-to-speech (Step 4)
edge-tts==7.2.8

# LLM / conversation (Step 5)
anthropic==0.121.0
